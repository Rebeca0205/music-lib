import styled from "styled-components";

const SongDetails = styled.section`
    align-items: center;
`

const BackButton = styled.button`
    margin-top: 30px;
`

export {
    SongDetails,
    BackButton
}